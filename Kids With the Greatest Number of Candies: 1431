class Solution:
    def kidsWithCandies(self, candies, extraCandies):
        output = []
        for item in candies:
            if item + extraCandies >= max(candies):
                output.append(True)
            else:
                output.append(False)
        return output
